{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @iframes/* --parallel start",
    "build": "lerna run --scope @iframes/* build",
    "clean": "lerna run --scope @iframes/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
