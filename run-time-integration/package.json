{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @run-time-integration/* --parallel start",
    "build": "lerna run --scope @run-time-integration/* build",
    "clean": "lerna run --scope @run-time-integration/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
