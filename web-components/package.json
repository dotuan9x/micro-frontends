{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @web-components/* --parallel start",
    "build": "lerna run --scope @web-components/* build",
    "clean": "lerna run --scope @web-components/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
