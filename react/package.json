{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @react/* --parallel start",
    "build": "lerna run --scope @react/* build",
    "clean": "lerna run --scope @react/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
