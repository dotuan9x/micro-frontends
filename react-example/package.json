{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @react-example/* --parallel start",
    "build": "lerna run --scope @react-example/* build",
    "clean": "lerna run --scope @react-example/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
